node_modules/
_site/
.DS_Store

# Generated slur/word list for the username moderator (a blocklist dump doesn't
# belong in the repo). Rebuild with `npm run gen:blocklist`.
docs/blocklist-seed.sql
build.log

# The private half of the split (2026-08-12). Its own git repo, its own private
# remote — the canon, the roadmaps and the backlog. Never tracked here.
private/
